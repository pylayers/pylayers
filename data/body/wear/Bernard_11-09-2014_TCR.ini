; device id
[34]
; device status : simulated or real
status = real
; dynamic|static
typ = static
; device name (see device.json)
name= TCR
; name of the marker
radiomarkname = RHead
;antenna file ( soon deprecated-information available in device.json)
file = S17R2m.sh3
; rotation matrix of antenna
T = np.array([[1,0,0],[0,1,0],[0,0,1]])
; comentaries
comment = 1 Right Head

; device id
[37]
; device status : simulated or real
status = real
; dynamic|static
typ = static
; device name (see device.json)
name= TCR
; name of the marker
radiomarkname = LAnk
;antenna file ( soon deprecated-information available in device.json)
file = S17R2m.sh3
; rotation matrix of antenna
T = np.array([[1,0,0],[0,1,0],[0,0,1]])
; comentaries
comment = Left ankle

; device id
[33]
; device status : simulated or real
status = real
; dynamic|static
typ = static
; device name (see device.json)
name= TCR
; name of the marker
radiomarkname = LKnee
;antenna file ( soon deprecated-information available in device.json)
file = S17R2m.sh3
; rotation matrix of antenna
T = np.array([[1,0,0],[0,1,0],[0,0,1]])
; comentaries
comment = Left knee

; device id
[48]
; device status : simulated or real
status = real
; dynamic|static
typ = static
; device name (see device.json)
name= TCR
; name of the marker
radiomarkname = LWrist
;antenna file ( soon deprecated-information available in device.json)
file = S17R2m.sh3
; rotation matrix of antenna
T = np.array([[1,0,0],[0,1,0],[0,0,1]])
; comentaries
comment = Left wrist

; device id
[36]
; device status : simulated or real
status = real
; dynamic|static
typ = static
; device name (see device.json)
name= TCR
; name of the marker
radiomarkname = RAnk
;antenna file ( soon deprecated-information available in device.json)
file = S17R2m.sh3
; rotation matrix of antenna
T = np.array([[1,0,0],[0,1,0],[0,0,1]])
; comentaries
comment = Right Ankle

; device id
[26]
; device status : simulated or real
status = real
; dynamic|static
typ = static
; device name (see device.json)
name= TCR
; name of the marker
radiomarkname = RWrist
;antenna file ( soon deprecated-information available in device.json)
file = S17R2m.sh3
; rotation matrix of antenna
T = np.array([[1,0,0],[0,1,0],[0,0,1]])
; comentaries
comment = Right wrist

; device id
[25]
; device status : simulated or real
status = real
; dynamic|static
typ = static
; device name (see device.json)
name= TCR
; name of the marker
radiomarkname = RFChest
;antenna file ( soon deprecated-information available in device.json)
file = S17R2m.sh3
; rotation matrix of antenna
T = np.array([[1,0,0],[0,1,0],[0,0,1]])
; comentaries
comment = 1 Right upper Chest

; device id
[30]
; device status : simulated or real
status = real
; dynamic|static
typ = static
; device name (see device.json)
name= TCR
; name of the marker
radiomarkname = LFChest
;antenna file ( soon deprecated-information available in device.json)
file = S17R2m.sh3
; rotation matrix of antenna
T = np.array([[1,0,0],[0,1,0],[0,0,1]])
; comentaries
comment = 1 Left upper Chest



; device id
[2]
; device status : simulated or real
status = real
; dynamic|static
typ = static
; device name (see device.json)
name= TCR
; name of the marker[]
radiomarkname = LChest
;antenna file ( soon deprecated-information available in device.json)
file = S17R2m.sh3
; rotation matrix of antenna
T = np.array([[1,1,0],[0,1,0],[0,0,1]])
; comentaries
comment = 1 Left Chest

; device id
[35]
; device status : simulated or real
status = real
; dynamic|static
typ = static
; device name (see device.json)
name= TCR
; name of the marker[]
radiomarkname = BChest
;antenna file ( soon deprecated-information available in device.json)
file = S17R2m.sh3
; rotation matrix of antenna
T = np.array([[1,0,0],[0,1,0],[0,0,1]])
; comentaries
comment = 1 Back Center
